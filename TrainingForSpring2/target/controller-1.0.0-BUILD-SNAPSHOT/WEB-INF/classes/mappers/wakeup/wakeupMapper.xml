<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.happylopers.mapper.wakeup.wakeupMapper">
    <select id="list" resultType="WakeUpVO">
    <![CDATA[
	select
    seq, date, comment
    from daily_log
    where  
    DATE_FORMAT(date,'%Y%m%d') < DATE_FORMAT(date_add(now(), interval 1 month), '%Y%m%d')
    AND 
    DATE_FORMAT(date,'%Y%m%d') >= DATE_FORMAT(CONCAT(DATE_FORMAT(NOW(),'%Y%m'),'01'), '%Y%m%d')
    order by date
    ]]>
    </select>
    
    <insert id="create">
        insert into daily_log (date, comment)
        values(now(),#{comment})
    </insert>
    
    <delete id="delete">
        delete from daily_log where seq = #{seq}
    </delete>
    
    <update id="update">
        update daily_log 
        set 
        date = #{date}, 
        comment = #{comment}
        where seq = #{seq}
    </update>
</mapper>